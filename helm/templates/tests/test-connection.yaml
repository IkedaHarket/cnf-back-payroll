apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-portal-privado.fullname" . }}-test-connection"
  labels:
    {{- include "backend-portal-privado.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-portal-privado.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
