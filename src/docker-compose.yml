# Please refer https://aka.ms/HTTPSinContainer on how to setup an https developer certificate for your ASP .NET Core service.

version: '3.4'

services:
  backend-ProyectName:
    image: backend-ProyectName:qa
    container_name: backend-ProyectName
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5200:80
    environment:
      - ASPNETCORE_ENVIRONMENT=QA
      - Apis:UrlClientes=http://api-clientes.qa.seguros.local/clientes/
      - Apis:UrlProductos=http://api-productos.qa.seguros.local/productos/
      - API_BASE_PATH=/backend-ProyectName
